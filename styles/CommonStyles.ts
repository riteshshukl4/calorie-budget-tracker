import { StyleSheet } from 'react-native';

export const CommonStyles = StyleSheet.create({
  infoContainer: {
    marginVertical: 10,
    padding: 15,
    borderRadius: 10,
    elevation: 2,
  },
  infoText: {
    fontSize: 16,
    fontWeight: 'bold',
  },
  infoValue: {
    fontSize: 18,
    fontWeight: 'bold',
    marginTop: 5,
  },
  itemText: {
    fontSize: 16,
    fontWeight: 'bold',
  },
  itemSubText: {
    fontSize: 14,
    color: 'gray',
  },
});